(jbuild_version 1)

(library
 ((name mirage_nat)
  (public_name mirage-nat)
  (modules (mirage_nat nat_rewrite nat_packet))
  (wrapped false)
  (libraries (cstruct tcpip.ipv4 tcpip.tcp tcpip.udp tcpip.icmpv4 mirage-clock-lwt mirage-time-lwt))
  (preprocess (pps (ppx_deriving.eq)))
  )
)
